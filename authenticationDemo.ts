let credential = {

    "userName": "user123",
    "password": "fluffyBunny"
} ;

function authenticate(credential) : boolean {
    return credential.userName == 'user123' && credential.password == 'sdlfkjsdflkj' ;
}
